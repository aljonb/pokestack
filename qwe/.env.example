# PocketBase Configuration
# The URL where your PocketBase server is running
PUBLIC_PB_URL=http://127.0.0.1:8090

# Admin credentials for database setup (used by npm run setup)
# These are NOT stored in the app, only used for initial provisioning
PB_ADMIN_EMAIL=
PB_ADMIN_PASS=

# Google OAuth2 Credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
